package TP.interfaz;

import TP.archivos.Archivo;
import TP.estadistica.StatisticsManager;
import TP.estadistica.mapa.InfoMapa;
import de.fhpotsdam.unfolding.geo.Location;
import TP.entidades.Fecha;
import TP.estadistica.Zona;
import TP.estadistica.mapa.BuscadorCoordenadas;
import TP.notificaciones.ContactNotification;
import TP.usuarios.Admin;
import TP.usuarios.Ciudadano;
import TP.util.Check;

import java.util.ArrayList;

public class Tester {
    public static void main(String[] args) {

        StatisticsManager stats = new StatisticsManager();
        stats.mostrarMapa();













    }
}
